<Window x:Class="FinancialManagerApp.Views.EditGoalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edytuj Skarbonkę" Height="500" Width="450" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        Background="#ECF0F1">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Ustawienia Skarbonki" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2C3E50"/>

            <TextBlock Text="Nazwa celu:" FontSize="12" Foreground="#7F8C8D"/>
            <TextBox x:Name="GoalNameBox" Text="{Binding GoalName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

            <TextBlock Text="Kwota docelowa (zł):" FontSize="12" Foreground="#7F8C8D"/>
            <TextBox x:Name="TargetAmountBox" Text="{Binding TargetAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

            <GroupBox Header="Automatyzacja (wpłaty cykliczne)" Margin="0,0,0,20" Padding="10">
                <StackPanel>
                    <CheckBox x:Name="IsRecurringCheck" Content="Włącz wpłaty cykliczne" IsChecked="{Binding IsRecurring}" Margin="0,5"/>
                    <StackPanel IsEnabled="{Binding IsChecked, ElementName=IsRecurringCheck}">
                        <TextBlock Text="Typ pobierania:" Margin="0,5,0,0" FontSize="11"/>
                        <ComboBox x:Name="TypeCombo" Margin="0,2,0,10">
                            <ComboBoxItem Content="kwota"/>
                            <ComboBoxItem Content="procent"/>
                        </ComboBox>
                        <TextBlock Text="Wartość (PLN lub %):" FontSize="11"/>
                        <TextBox x:Name="ValueBox" Text="{Binding ContributionValue, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0" Padding="3"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <UniformGrid Columns="2">
                <Button Content="Zapisz zmiany" Click="BtnSave_Click" Background="#1ABC9C" Foreground="White" Margin="0,0,5,0" Padding="10" FontWeight="Bold" BorderThickness="0"/>
                <Button Content="Anuluj" IsCancel="True" Margin="5,0,0,0" Padding="10" Background="Transparent" BorderBrush="#BDC3C7"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</Window>