<UserControl x:Class="FinancialManagerApp.Views.WalletsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:models="clr-namespace:FinancialManagerApp.Models"
             
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#ECF0F1">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Zarządzanie Portfelami" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>

            <Button Content="+ Dodaj nowy portfel" 
                    Command="{Binding OpenAddWalletCommand}"
                    Background="#1ABC9C" Foreground="White" Margin="30,0,0,0" Padding="15,8" FontWeight="SemiBold" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" BorderBrush="#D5DBDB" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Wallets}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      Background="Transparent" 
                      BorderThickness="0" 
                      RowHeight="50" 
                      VerticalContentAlignment="Center"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#ECF0F1"
                      VerticalGridLinesBrush="Transparent">

                <d:DataGrid.ItemsSource>
                    <x:Array Type="{x:Type models:WalletModel}">
                        <models:WalletModel Name="Portfel Gotówkowy" Type="Manualny" Description="Pieniądze w portfelu fizycznym" Balance="250.00"/>
                        <models:WalletModel Name="Konto Revolut" Type="API" Description="Konto walutowe (Synchronizacja auto)" Balance="1450.99"/>
                        <models:WalletModel Name="Skarbonka Domowa" Type="Manualny" Description="Oszczędności w gotówce" Balance="5000.00"/>
                    </x:Array>
                </d:DataGrid.ItemsSource>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nazwa konta" Binding="{Binding Name}" Width="1.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="5,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="120" Foreground="#7F8C8D"/>

                    <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="2*" Foreground="#95A5A6"/>

                    <DataGridTextColumn Header="Saldo" Binding="{Binding Balance, StringFormat={}{0:N2} zł}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="5,0"/>
                                <Setter Property="Foreground" Value="#2C3E50"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Akcje" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edytuj" Background="#F39C12" Foreground="White" Margin="2" Padding="8,4" FontSize="11" FontWeight="SemiBold"/>

                                    <Button Content="Usuń" Background="#E74C3C" Foreground="White" Margin="2" Padding="8,4" FontSize="11" FontWeight="SemiBold"/>

                                    <Button Content="Odśwież" 
                                    Command="{Binding RefreshWalletCommand}"
                                    CommandParameter="{Binding}"
                                    Foreground="White" 
                                    Margin="2" 
                                    Padding="8,4" 
                                    FontSize="11" 
                                    FontWeight="SemiBold">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#3498DB"/>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="Manualny">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>