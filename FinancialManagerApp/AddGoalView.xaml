<Window x:Class="FinancialManagerApp.Views.AddGoalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Nowa Skarbonka" Height="550" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#ECF0F1">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Dodaj Nowy Cel" FontSize="20" FontWeight="Bold" 
                   Foreground="#2C3E50" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
            <StackPanel>
                <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#D5DBDB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Nazwa celu (np. Wakacje)" FontSize="11" Foreground="#7F8C8D"/>
                        <TextBox x:Name="GoalNameBox" Padding="5" Margin="0,5,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,5,0">
                                <TextBlock Text="Kwota docelowa" FontSize="11" Foreground="#7F8C8D"/>
                                <TextBox x:Name="TargetAmountBox" Text="0,00" Padding="5" Margin="0,5,0,0" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="Saldo startowe" FontSize="11" Foreground="#7F8C8D"/>
                                <TextBox x:Name="CurrentAmountBox" Text="0,00" Padding="5" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,2,0" BorderBrush="#D5DBDB" BorderThickness="1">
                    <StackPanel>
                        <CheckBox x:Name="IsRecurringCheck" Content="Automatyczne oszczędzanie (%)" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <StackPanel IsEnabled="{Binding IsChecked, ElementName=IsRecurringCheck}">
                            <TextBlock Text="Portfel źródłowy" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <ComboBox x:Name="SourceWalletCombo" 
                      ItemsSource="{Binding AvailableWallets}" 
                      DisplayMemberPath="Name" 
                      SelectedValuePath="Id" 
                      SelectedIndex="0" 
                      Margin="0,0,0,10" 
                      FontWeight="SemiBold"/>

                            <TextBlock Text="Procent od każdego wpływu (%)" FontSize="11" Foreground="#7F8C8D"/>
                            <TextBox x:Name="ValueBox" Text="0" Padding="5" ToolTip="Wpisz wartość od 1 do 100"/>
                            <TextBlock Text="Pieniądze będą automatycznie pobierane przy każdym przychodzie na wybrany portfel." 
                       FontSize="10" Foreground="#95A5A6" TextWrapping="Wrap" Margin="0,5,0,0" FontStyle="Italic"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <UniformGrid Grid.Row="2" Columns="2">
            <Button Content="Anuluj" IsCancel="True" Padding="10" Margin="0,0,5,0" 
                    Background="Transparent" BorderBrush="#BDC3C7" BorderThickness="1"/>
            <Button Content="Utwórz Skarbonkę" Click="BtnCreate_Click" 
                    Background="#1ABC9C" Foreground="White" Padding="10" FontWeight="Bold" BorderThickness="0"/>
        </UniformGrid>
    </Grid>
</Window>